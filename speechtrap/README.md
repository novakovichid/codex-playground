# SpeechTrap 🎯

Электронная версия популярной настольной игры Taboo. Игроки описывают слова, не используя запрещенные слова, а другие участники должны их угадать. Этот каталог содержит весь исходный код и окружение проекта SpeechTrap внутри мультипроектного репозитория.

## 🎮 Особенности игры

- **Многопользовательские комнаты** - до 8 игроков
- **Реальное время** - синхронизация через WebSockets  
- **Различные сложности слов** - базовая, средняя, сложная
- **Система рейтингов** - отслеживание статистики игроков
- **Чат** - общение между игроками
- **Настраиваемые параметры** - время раунда, количество раундов, сложность

## 🏗️ Архитектура

### Backend (FastAPI + Python)
- **FastAPI** - современный веб-фреймворк
- **SQLModel/SQLAlchemy** - ORM для работы с БД
- **WebSockets** - для реального времени
- **JWT** - аутентификация
- **Pytest** - тестирование

### Frontend (React + TypeScript)
- **React 19** - пользовательский интерфейс
- **TypeScript** - типизация
- **Vite** - сборщик
- **Tailwind CSS** - стилизация
- **Radix UI** - компоненты UI

### База данных
- **PostgreSQL** - основная БД
- **Отдельная тестовая БД** - для тестов
- **SQLite по умолчанию** - локальный файл `backend/speechtrap.db` создаётся автоматически, если переменные окружения для PostgreSQL не заданы.

## 📁 Структура проекта

```
speechtrap/
├── backend/
│   ├── app/
│   │   ├── api/       # API роутеры
│   │   ├── core/      # Базовые функции
│   │   ├── db/        # База данных
│   │   ├── models/    # SQLModel модели
│   │   ├── schemas/   # Pydantic схемы
│   │   └── main.py    # Главный файл
│   └── tests/         # Тесты
├── frontend/
│   ├── src/
│   │   ├── components/ # Страницы и компоненты
│   │   └── utils/      # Различные функции
│   └── public/
├── docker-compose.yml     # Для разработки
├── docker-compose.prod.yml # Для продакшена
└── requirements.txt       # Python зависимости
```

## 🚀 Запуск

### Подготовка окружения
- Установите Docker и Docker Compose.
- Свободные порты: 5432 (PostgreSQL), 8000 (backend), 5173 (frontend dev) и 4173 (frontend preview).

### Локальная разработка
1. Перейдите в каталог проекта (если запускаете из корня репозитория): `cd speechtrap`.
2. Соберите и запустите сервисы: `docker-compose up --build`.
3. API будет доступен на `http://localhost:8000`, фронтенд — на `http://localhost:5173`.
4. При изменениях в коде контейнеры обновятся автоматически благодаря volume-монтажам.

### Продакшен-режим (локально)
1. Соберите образы и запустите их без hot-reload: `docker-compose -f docker-compose.prod.yml up --build`.
2. API: `http://localhost:8000`, фронтенд preview: `http://localhost:4173`.

### Частые проблемы

**Ошибка:** `unable to get image 'postgres:15': error during connect: Get "http://%2F%2F.%2Fpipe%2FdockerDesktopLinuxEngine/...": open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified.`

Это означает, что Docker CLI не может подключиться к демонy Docker Desktop (обычно на Windows/WSL). Попробуйте следующие шаги:

1. Запустите Docker Desktop и дождитесь состояния **Engine running**.
2. Если работаете через WSL, убедитесь, что используется WSL2 и включена интеграция для вашей дистрибуции (Settings → Resources → WSL integration).
3. Сбросьте контекст и ручные настройки хоста, чтобы использовать стандартный демон:
   ```bash
   docker context use default
   unset DOCKER_HOST
   docker info   # команда должна успешно подключиться
   ```
4. После восстановления подключения перезапустите `docker-compose up --build`.

## 🎯 Игровой процесс

1. **Регистрация/Вход** - создайте аккаунт или войдите
2. **Создание комнаты** - настройте параметры игры
3. **Приглашение друзей** - поделитесь кодом комнаты
4. **Игра** - описывайте слова и угадывайте
5. **Результаты** - просматривайте статистику

